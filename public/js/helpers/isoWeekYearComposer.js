/**
 * Created by romab on 23.02.2016.
 */
define(['moment'], function (moment) {
    "use strict";
    var composer = function (year, month, week, wTrackObject) {
        var momentObject;
        var firstNotZeroDay;
        var isoYear;

        if (month !== 1 && month !== 12){
            return year;
        }


    };

    return composer;
});